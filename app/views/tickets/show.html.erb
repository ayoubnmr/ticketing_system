<h1>show</h1>
<div class="ticket">
  <header>
    <h1><%= @project.name %></h1>
       <%= current_user.email%>
       <%= current_user.id%>
       <%= current_user.password%>
       <%= current_user.id%>
       <%= @project.end%>
       <%= User.all%>
  </header>

  <header>
    <h2><%= @ticket.name %></h2>
  </header>
<%- @project.tickets.each do |ticket|%>
                     <%= ticket.name%><br>
                     <%= ticket.title%><br>
                     <br>
                     <%= ticket.content%>
                      <br>
                   <% end %>
</div>
